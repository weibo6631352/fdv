# 05 验收测试计划任务

## 覆盖范围

- 设计文档 `4.4 测试重点`
- 设计文档 `5.4 代码审查与测试规范`
- 根 README 的开发阶段规则

## 阶段说明

开发过程中不要求补充或运行验证测试。本任务只维护专项验收阶段的测试清单、场景和责任归属，避免开发 worker 因测试执行互相阻塞。

## 验收清单

- Crypto 分类先于 FDV / 500M 匹配。
- FDV event + 500M market 才能进入 target pool。
- 非 500M 阈值被排除。
- `NO best ask > 0.60` 不触发买入。
- FAK partial fill 只对成交 shares 挂 SELL。
- FAK no fill 会释放预算并重新分配。
- open BUY 异常会触发 cancel。
- 等权分配不会让早发现 market 超过 `max_market_usdc`。
- User WS 断线后恢复必须完成 reconcile 才允许继续买入。
- DB 写入失败不阻塞订单提交，但 outbox 可重试。
- Admin API 大查询、数据库慢写入、日志落盘变慢时，FAK BUY 提交路径仍不被阻塞。
- P0 交易队列优先于 P2 / P3 队列执行，低优先级任务积压不会占用交易线程池。
- 交易热路径遇到低优先级锁等待超时时，会跳过非关键操作并记录告警。
- 高频 orderbook 更新可以合并非关键快照，但首次价格触发事件不能丢失。
- 性能验收覆盖 `entry_signal_to_submit_ms`、`trading_queue_depth` 和 `trading_lock_wait_ms` 阈值。

## 交付物

- 验收测试矩阵：场景、归属项目组、前置数据、期望事件、期望状态、是否性能相关。
- 验收阶段执行顺序建议：domain 规则、交易流程、运行时并发、持久化降级、Admin 人工操作、端到端恢复。
- 测试缺口登记表，供后续专项验收使用。

## 并行接口

- 上游：所有项目组提交实现与注释说明。
- 下游：专项验收 worker。
- 与 `README.md` 中“开发过程中不要求补充或运行验证测试”保持一致。

## 注释要求

本任务不要求实现测试代码；如后续进入验收阶段，再按本清单补充测试说明和必要注释。

